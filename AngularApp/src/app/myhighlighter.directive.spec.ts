import { MyhighlighterDirective } from './myhighlighter.directive';

describe('MyhighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new MyhighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
